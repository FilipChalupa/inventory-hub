# Web (Vite) — all variables are optional for local development.

# Where the Vite dev server proxies /api, /health and /auth during `npm run dev`.
# Defaults to the local backend; point it elsewhere to develop the web app
# against a remote/staging server.
VITE_API_PROXY=http://localhost:3001
